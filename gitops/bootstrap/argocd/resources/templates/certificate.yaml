apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-tls
  namespace: istio-system
spec:
  secretName: argocd-tls
  issuerRef:
    name: {{ include "argocd-resources.issuer" . }}
    kind: ClusterIssuer
  dnsNames:
    - {{ include "argocd-resources.host" . }}
