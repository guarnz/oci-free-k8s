apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zitadel-tls
  namespace: istio-system
spec:
  secretName: zitadel-tls
  issuerRef:
    name: {{ include "zitadel-resources.issuer" . }}
    kind: ClusterIssuer
  dnsNames:
    - {{ include "zitadel-resources.host" . }}
